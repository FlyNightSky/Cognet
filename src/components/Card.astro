---

	function openIframe(elementName) {
  if(name == "web.ngms.place"){
		var url = 'www.youtube.com'

				var win = window.open();
				win.document.body.style.margin = '0';
				win.document.body.style.height = '100vh';
				win.document.title = "Proxy"
				var iframe = win.document.createElement('iframe');
				iframe.style.border = 'none';
				iframe.style.width = '100%';
				iframe.style.height = '100%';
				iframe.style.margin = '0';
				iframe.src = url;
				win.document.body.appendChild(iframe);
        window.close() ;
	}
}

export interface Props {
	title: string;
	body: string;
	href: string;
  img: string;
}

const { by="hi", title="hi", body="hi", img ="/banner1.svg", id="elementName" } = Astro.props as Props;
---

 <div class=" ml-[.5rem] mt-[2rem] h-fit max-w-sm bg-red border border-gray-200 rounded-lg shadow mr-[.5rem]  lg:ml-[2rem] xl:ml-[2rem] lg:mt-[3rem] xl:mt-[3rem]">
    <a onclick="openIframe('{id}')">
        <img class="rounded-t-lg" src={img} alt="" />
    </a>
    <div class="p-5">
        <a onclick="openIframe('{id}')">
            <h5 class="mb-2 text-2xl font-bold tracking-tight text-white">{title}</h5>
        </a>
        <p class="mb-3 font-normal text-gray-400">{body}<br><br>
        Proxy By {by}
        </p>
        <a onclick="openIframe('{id}')" class="inline-flex items-center px-3 py-2 text-sm font-medium text-center text-white bg-blue-700 rounded-lg hover:bg-blue-800 focus:ring-4 focus:outline-none focus:ring-blue-300 dark:bg-blue-600 dark:hover:bg-blue-700 dark:focus:ring-blue-800">
            Go to
            <svg aria-hidden="true" class="w-4 h-4 ml-2 -mr-1" fill="currentColor" viewBox="0 0 20 20" xmlns="http://www.w3.org/2000/svg"><path fill-rule="evenodd" d="M10.293 3.293a1 1 0 011.414 0l6 6a1 1 0 010 1.414l-6 6a1 1 0 01-1.414-1.414L14.586 11H3a1 1 0 110-2h11.586l-4.293-4.293a1 1 0 010-1.414z" clip-rule="evenodd"></path></svg>
        </a>
    </div>
</div>
